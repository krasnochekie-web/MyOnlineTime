<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/backgroundDark"
    android:paddingTop="@dimen/header_clearance">
    <!-- НОВАЯ ВЕРХНЯЯ ПАНЕЛЬ (Без наложений) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingBottom="16dp"
        android:gravity="center_vertical">

        <!-- Вертикальный блок для кнопок (слева) -->
        <LinearLayout
            android:id="@+id/buttons_panel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Кнопка "График" -->
            <TextView
                android:id="@+id/btn_chart"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_button_dark"
                android:text="@string/btn_chart_text"
                android:textColor="@color/textWhite"
                android:textSize="14sp"
                android:textStyle="bold"
                android:drawableLeft="@drawable/ic_chart_yellow"
                android:drawablePadding="6dp"
                android:paddingLeft="12dp"
                android:paddingRight="12dp"
                android:paddingTop="6dp"
                android:paddingBottom="6dp"
                android:layout_marginBottom="8dp" 
                android:gravity="center_vertical" />

            <!-- Кнопка "За все время" -->
            <TextView
                android:id="@+id/btn_all_time"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_button_dark"
                android:text="@string/btn_all_time_text"
                android:textColor="@color/textWhite"
                android:textSize="14sp"
                android:textStyle="bold"
                android:drawableLeft="@drawable/ic_star_yellow"
                android:drawablePadding="6dp"
                android:paddingLeft="12dp"
                android:paddingRight="12dp"
                android:paddingTop="6dp"
                android:paddingBottom="6dp"
                android:gravity="center_vertical" />
        </LinearLayout>

        <!-- Прозрачная пружина (выталкивает спиннер вправо) -->
        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1"/>

        <!-- Спиннер (справа) -->
        <Spinner
            android:id="@+id/spinner_period"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@null"
            android:padding="5dp"
            android:popupBackground="@drawable/bg_spinner_popup"
            android:spinnerMode="dropdown" />
    </LinearLayout>
    <!-- ГЛАВНЫЙ СКРОЛЛ (Остается без изменений) -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="@dimen/bottom_nav_clearance">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- КАРТОЧКА ОБЩЕГО ВРЕМЕНИ -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_app_card"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:padding="24dp"
                android:layout_marginLeft="16dp"
                android:layout_marginRight="16dp"
                android:layout_marginBottom="16dp">

                <TextView
                    android:text="@string/total_time"
                    android:textColor="@color/textWhite"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"/>
                <View
                    android:layout_width="0dp"
                    android:layout_height="1dp"
                    android:layout_weight="1"/>
                <TextView
                    android:id="@+id/text_total_time_sum"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="..."
                    android:textColor="@color/burgundyRed"
                    android:textSize="16sp"
                    android:textStyle="bold"/>
            </LinearLayout>

            <!-- СПИСОК ПРИЛОЖЕНИЙ -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/apps_list"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingLeft="16dp"
                android:paddingRight="16dp"
                android:nestedScrollingEnabled="false"
                android:clipToPadding="false"
                android:scrollbars="none"/>
        </LinearLayout>
    </androidx.core.widget.NestedScrollView>
</LinearLayout>
